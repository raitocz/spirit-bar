{
  "name": "spirit-bar",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler d1 migrations apply spirit-bar-db --remote && wrangler deploy",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.19",
    "@cloudflare/workers-types": "^4",
    "typescript": "^5",
    "vitest": "^3.2.4",
    "wrangler": "^4"
  }
}
